[package]
name = "diffler-demo"
version = "0.1.0"
edition = "2021"

[dependencies]
ratatui = "0.30"
crossterm = "0.29"
similar = "2"

[profile.release]
strip = true

# standalone demo, not part of the diffler workspace
[workspace]
